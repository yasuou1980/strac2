[theme]
primaryColor = "#FF6B6B"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#262730"

[server]
headless = true
port = 8501
# CORS / XSRF 保護はデフォルト(有効)のまま。公開デプロイ時の CSRF 対策として無効化しない。
